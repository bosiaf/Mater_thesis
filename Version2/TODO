dynamic propagation function
 - latent cells dynamics DONE!

evolution function
 - all DONE!

test

write main file

host with different spreading rate DONE

Fix fitness

write new_host_infection(mt19937 rng, string file);
